services:
  db:
    image: postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_USER: opencut
      POSTGRES_PASSWORD: opencut
      POSTGRES_DB: opencut
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U opencut"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 10s

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 10s

  serverless-redis-http:
    image: hiett/serverless-redis-http:latest
    ports:
      - "8079:80"
    environment:
      SRH_MODE: env
      SRH_TOKEN: example_token
      SRH_CONNECTION_STRING: "redis://redis:6379"
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget --spider -q http://127.0.0.1:80 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 10s

  # --- AI Services ---

  ollama:
    image: ollama/ollama:latest
    restart: unless-stopped
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    healthcheck:
      test: ["CMD", "ollama", "list"]
      interval: 15s
      timeout: 10s
      retries: 10
      start_period: 30s

  ai-backend:
    build:
      context: ./services/ai-backend
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "8420:8420"
    environment:
      - OPENCUT_OLLAMA_URL=http://ollama:11434
      - OPENCUT_PORT=8420
      - OPENCUT_CORS_ORIGINS=["http://localhost:3000","http://localhost:3100","http://localhost:5173"]
      - OPENCUT_OLLAMA_DEFAULT_MODEL=llama3.2:1b
      - OPENCUT_OLLAMA_TIMEOUT=300
      - OPENCUT_WHISPER_MODEL_SIZE=${WHISPER_MODEL_SIZE:-base}
      - OPENCUT_WHISPER_DEVICE=cpu
      - OPENCUT_WHISPER_SERVICE_URL=http://whisper-service:8421
      - OPENCUT_TTS_SERVICE_URL=http://tts-service:8422
      - OPENCUT_IMAGE_SERVICE_URL=http://image-service:8423
    volumes:
      - ai_models:/app/models
      - ai_generated:/app/generated
    depends_on:
      ollama:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8420/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

  whisper-service:
    build:
      context: ./services/whisper-service
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "8421:8421"
    environment:
      - WHISPER_MODEL_SIZE=${WHISPER_MODEL_SIZE:-base}
      - WHISPER_DEVICE=cpu
      - WHISPER_COMPUTE_TYPE=int8
    volumes:
      - whisper_models:/root/.cache
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8421/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 15s

  tts-service:
    build:
      context: ./services/tts-service
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "8422:8422"
    volumes:
      - tts_models:/root/.cache
    environment:
      - TTS_AUTOLOAD=true
    deploy:
      resources:
        limits:
          memory: 6g
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8422/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

  image-service:
    build:
      context: ./services/image-service
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "8423:8423"
    volumes:
      - image_models:/root/.cache
      - image_generated:/app/generated
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8423/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 15s

  # --- Web App ---

  web:
    build:
      context: .
      dockerfile: ./apps/web/Dockerfile
      args:
        - FREESOUND_CLIENT_ID=${FREESOUND_CLIENT_ID}
        - FREESOUND_API_KEY=${FREESOUND_API_KEY}
    restart: unless-stopped
    ports:
      - "3100:3000"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=postgresql://opencut:opencut@db:5432/opencut
      - BETTER_AUTH_SECRET=your-production-secret-key-here
      - UPSTASH_REDIS_REST_URL=http://serverless-redis-http:80
      - UPSTASH_REDIS_REST_TOKEN=example_token
      - NEXT_PUBLIC_SITE_URL=http://localhost:3100
      - NEXT_PUBLIC_MARBLE_API_URL=https://api.marblecms.com
      - NEXT_PUBLIC_AI_BACKEND_URL=http://localhost:8420
      - MARBLE_WORKSPACE_KEY=${MARBLE_WORKSPACE_KEY:-placeholder}
      - FREESOUND_CLIENT_ID=${FREESOUND_CLIENT_ID}
      - FREESOUND_API_KEY=${FREESOUND_API_KEY}
      # Transcription (Optional - leave blank to disable auto-captions)
      - CLOUDFLARE_ACCOUNT_ID=${CLOUDFLARE_ACCOUNT_ID:-placeholder}
      - R2_ACCESS_KEY_ID=${R2_ACCESS_KEY_ID:-placeholder}
      - R2_SECRET_ACCESS_KEY=${R2_SECRET_ACCESS_KEY:-placeholder}
      - R2_BUCKET_NAME=${R2_BUCKET_NAME:-opencut-transcription}
      - MODAL_TRANSCRIPTION_URL=${MODAL_TRANSCRIPTION_URL:-http://localhost:0}
      - NEXT_PUBLIC_WHISPER_SERVICE_URL=http://localhost:8421
      - NEXT_PUBLIC_TTS_SERVICE_URL=http://localhost:8422
      - NEXT_PUBLIC_IMAGE_SERVICE_URL=http://localhost:8423
    depends_on:
      db:
        condition: service_healthy
      serverless-redis-http:
        condition: service_healthy
      ai-backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:3000/api/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

volumes:
  postgres_data:
  ollama_data:
  ai_models:
  ai_generated:
  whisper_models:
  tts_models:
  image_models:
  image_generated:

networks:
  default:
    name: opencut-network
